@page "/settings"

@using System.Text.Json
@using System.Text.RegularExpressions
@using vrScraper.DB.Models
@using vrScraper.Components
@using vrScraper.Services

@using Microsoft.AspNetCore.Identity
@inject IVideoService VideoService
@inject ISettingService SettingService
@inject IScraperRegistry ScraperRegistry
@inject ITagNormalizationService TagNormalizationService
@inject IOllamaService OllamaService

<h3>Settings</h3>

<div class="settings-container">

    <!-- Tag Blacklist Section -->
    <div class="settings-section">
        <h5 class="section-title">
            <i class="icon-ban me-2"></i>
            Content Filtering
        </h5>

        <div class="setting-card">
            <label class="form-label fw-semibold mb-3">
                <i class="icon-filter me-1"></i>
                Global Tag Blacklist
            </label>
            <MultiSelect IsMultiple="true" TItem="DbTag" Data="@AvailableTags"
                             TextField="@((item) => item.Name)"
                             ValueField="@((item) => item.Name)"
                             ShowSearch="true"
                             @bind-SelectedValues="TagBlacklist" />
            <small class="form-text text-muted mt-2">
                Select tags that should be filtered out from all video listings and API responses
            </small>
        </div>

    </div>

    <!-- Per-Site Configuration -->
    <div class="settings-section">
        <h5 class="section-title">
            <i class="icon-globe me-2"></i>
            Source Sites
        </h5>
        <div class="table-responsive">
            <table class="vr-table">
                <thead>
                    <tr>
                        <th>Site</th>
                        <th>Enabled</th>
                        <th>Min Duration</th>
                        <th>Auto-Scrape</th>
                        <th>Scrape Time</th>
                        <th>Max Pages</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var scraper in ScraperRegistry.GetAllScrapers())
                    {
                        var s = scraper.SiteName;
                        var isEnabled = !scraper.IsExperimental && GetSiteSetting(s, "Enabled", s == "eporner.com" ? "true" : "false") == "true";
                        var minDur = GetSiteSetting(s, "MinDuration", "0");
                        var autoEnabled = GetSiteSetting(s, "AutoScrapeEnabled", "false") == "true";
                        var autoTime = GetSiteSetting(s, "AutoScrapeTime", "02:00");
                        var autoMaxPages = GetSiteSetting(s, "AutoScrapeMaxPages", "50");
                        <tr class="@(!isEnabled ? "site-disabled" : "")">
                            <td>
                                <span class="fw-semibold">@scraper.DisplayName</span>
                                @if (scraper.IsExperimental)
                                {
                                    <span class="badge bg-warning text-dark ms-2">Experimental</span>
                                }
                            </td>
                            <td>
                                <div class="form-check form-switch mb-0">
                                    <input class="form-check-input" type="checkbox" checked="@isEnabled"
                                           disabled="@scraper.IsExperimental"
                                           @onchange="(e) => OnSiteCheckboxChanged(s, nameof(isEnabled), e)" />
                                </div>
                            </td>
                            <td>
                                <input type="number" class="form-control form-control-sm site-input-sm" min="0" max="3600"
                                       value="@minDur" @onchange="(e) => OnSiteInputChanged(s, nameof(minDur), e)" />
                            </td>
                            <td>
                                <div class="form-check form-switch mb-0">
                                    <input class="form-check-input" type="checkbox" checked="@autoEnabled"
                                           @onchange="(e) => OnSiteCheckboxChanged(s, nameof(autoEnabled), e)" />
                                </div>
                            </td>
                            <td>
                                <input type="text" class="form-control form-control-sm site-input-sm" placeholder="HH:MM"
                                       value="@autoTime" @onchange="(e) => OnSiteTimeChanged(s, e)" />
                            </td>
                            <td>
                                <input type="number" class="form-control form-control-sm site-input-sm" min="1" max="500"
                                       value="@autoMaxPages" @onchange="(e) => OnSiteInputChanged(s, nameof(autoMaxPages), e)" />
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- LLM Title Normalization -->
    <div class="settings-section">
        <h5 class="section-title">
            <i class="icon-cpu me-2"></i>
            LLM Title Normalization
        </h5>
        <div class="setting-card">
            <div class="d-flex align-items-center gap-3 mb-3">
                <span class="fw-semibold">Ollama Status:</span>
                @if (_ollamaChecking)
                {
                    <span class="badge bg-secondary">Checking...</span>
                }
                else if (_ollamaAvailable)
                {
                    <span class="badge bg-success">Connected</span>
                }
                else
                {
                    <span class="badge bg-danger">Not available</span>
                }
                <button class="btn btn-sm btn-outline-secondary" @onclick="CheckOllamaStatus">
                    Recheck
                </button>
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Ollama URL</label>
                <input type="text" class="form-control" value="@_ollamaUrl"
                       @onchange="OnOllamaUrlChanged" placeholder="http://localhost:11434" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Model</label>
                <input type="text" class="form-control" value="@_ollamaModel"
                       @onchange="OnOllamaModelChanged" placeholder="qwen2.5:3b" />
            </div>
            <small class="form-text text-muted">
                Install Ollama from <a href="https://ollama.com" target="_blank">ollama.com</a> and run:
                <code>ollama pull qwen2.5:3b</code>
            </small>
        </div>
    </div>

    <!-- Tag Synonym Editor -->
    <div class="settings-section disabled-section">
        <h5 class="section-title">
            <i class="icon-tag me-2"></i>
            Tag Normalization (Synonyms)
            <span class="badge bg-secondary ms-2">Disabled</span>
        </h5>
        <div class="setting-card">
            <small class="form-text text-muted mb-3 d-block">
                Map synonym tags to a canonical name. During scraping, synonyms will be replaced with the canonical tag.
            </small>
            @foreach (var entry in _synonymMap)
            {
                var canonical = entry.Key;
                <div class="synonym-row mb-2">
                    <div class="d-flex align-items-center gap-2 mb-1">
                        <strong class="text-nowrap">@canonical</strong>
                        <span class="text-muted">&larr;</span>
                        <span class="text-muted">@string.Join(", ", entry.Value)</span>
                        <button class="btn btn-sm btn-outline-danger ms-auto" disabled>Remove</button>
                    </div>
                </div>
            }
            <hr />
            <div class="d-flex gap-2 align-items-end flex-wrap">
                <div>
                    <label class="form-label fw-semibold">Canonical Tag</label>
                    <input type="text" class="form-control form-control-sm" disabled placeholder="e.g. Blonde" />
                </div>
                <div>
                    <label class="form-label fw-semibold">Synonyms (comma-separated)</label>
                    <input type="text" class="form-control form-control-sm" disabled placeholder="e.g. blond, blondes" />
                </div>
                <button class="btn btn-sm btn-primary" disabled>Add</button>
            </div>
            <div class="mt-3 d-flex gap-2">
                <button class="btn btn-primary" disabled>
                    <i class="icon-save me-1"></i> Save Synonyms
                </button>
                <button class="btn btn-warning" disabled>
                    Normalize All Existing Tags
                </button>
            </div>
        </div>
    </div>

    <!-- Change Password Section -->
    <div class="settings-section">
        <h5 class="section-title">
            <i class="icon-lock me-2"></i>
            Change Password
        </h5>

        <div class="settings-grid">
            <div class="setting-card password-card">
                <div class="password-form">
                    <div class="mb-3">
                        <label class="form-label fw-semibold">
                            <i class="icon-key me-1"></i>
                            Current Password
                        </label>
                        <input type="password" class="form-control" @bind="_oldPassword" placeholder="Enter current password" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-semibold">
                            <i class="icon-lock me-1"></i>
                            New Password
                        </label>
                        <input type="password" class="form-control" @bind="_newPassword" placeholder="Enter new password (min. 4 characters)" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-semibold">
                            <i class="icon-lock me-1"></i>
                            Confirm New Password
                        </label>
                        <input type="password" class="form-control" @bind="_confirmNewPassword" placeholder="Confirm new password" />
                    </div>
                    <button class="btn btn-primary" @onclick="ChangePassword" disabled="@_changingPassword">
                        @if (_changingPassword)
                        {
                            <span>Saving...</span>
                        }
                        else
                        {
                            <i class="icon-save me-1"></i>
                            <span>Save Password</span>
                        }
                    </button>
                    @if (!string.IsNullOrEmpty(_passwordMessage))
                    {
                        <div class="mt-3 alert @(_passwordSuccess ? "alert-success" : "alert-danger") py-2">
                            @_passwordMessage
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

</div>

@code {
    private List<DbTag> AvailableTags { get; set; } = new();
    private List<DbSetting> AllSettings { get; set; } = new();

    public IEnumerable<string> TagBlacklist
    {
        get
        {
            var s = AllSettings.FirstOrDefault(s => s.Key == "TagBlacklist");
            if (s == null) return Enumerable.Empty<string>();
            return JsonSerializer.Deserialize<List<string>>(s.Value ?? "[]") ?? new List<string>();
        }

        set
        {
            var s = AllSettings.FirstOrDefault(s => s.Key == "TagBlacklist");
            if (s == null) return;
            s.Value = JsonSerializer.Serialize(value);
            this.SettingService.UpdateSetting(s);
        }
    }

    private string GetSiteSetting(string site, string key, string defaultValue)
    {
        var settingKey = $"Site:{site}:{key}";
        var s = AllSettings.FirstOrDefault(s => s.Key == settingKey);
        return s?.Value ?? defaultValue;
    }

    private void SetSiteSetting(string site, string key, string value)
    {
        var settingKey = $"Site:{site}:{key}";
        var s = AllSettings.FirstOrDefault(s => s.Key == settingKey);
        if (s == null)
        {
            s = new DbSetting { Key = settingKey, Type = "System.String", Value = value };
            AllSettings.Add(s);
        }
        s.Value = value;
        SettingService.UpdateSetting(s);
        StateHasChanged();
    }

    private void OnSiteCheckboxChanged(string site, string field, ChangeEventArgs e)
    {
        var key = field switch
        {
            nameof(Boolean) => "Enabled",
            "isEnabled" => "Enabled",
            "autoEnabled" => "AutoScrapeEnabled",
            _ => field
        };
        SetSiteSetting(site, key, e.Value?.ToString()?.ToLowerInvariant() ?? "false");
    }

    private void OnSiteInputChanged(string site, string field, ChangeEventArgs e)
    {
        var key = field switch
        {
            "minDur" => "MinDuration",
            "autoMaxPages" => "AutoScrapeMaxPages",
            _ => field
        };
        SetSiteSetting(site, key, e.Value?.ToString() ?? "");
    }

    private void OnSiteTimeChanged(string site, ChangeEventArgs e)
    {
        var value = e.Value?.ToString() ?? "";
        if (!Regex.IsMatch(value, @"^([01]\d|2[0-3]):[0-5]\d$"))
            return;
        SetSiteSetting(site, "AutoScrapeTime", value);
    }

    // Ollama fields
    private bool _ollamaAvailable;
    private bool _ollamaChecking;
    private string _ollamaUrl = "http://localhost:11434";
    private string _ollamaModel = "qwen2.5:3b";

    protected override async Task OnInitializedAsync()
    {
        await LoadTags();
        await LoadSettings();
        _synonymMap = TagNormalizationService.GetSynonymMap();
        _ollamaUrl = AllSettings.FirstOrDefault(s => s.Key == "Ollama:Url")?.Value ?? "http://localhost:11434";
        _ollamaModel = AllSettings.FirstOrDefault(s => s.Key == "Ollama:Model")?.Value ?? "qwen2.5:3b";
        await CheckOllamaStatus();
    }

    private async Task CheckOllamaStatus()
    {
        _ollamaChecking = true;
        StateHasChanged();
        _ollamaAvailable = await OllamaService.IsAvailable();
        _ollamaChecking = false;
        StateHasChanged();
    }

    private async Task OnOllamaUrlChanged(ChangeEventArgs e)
    {
        _ollamaUrl = e.Value?.ToString() ?? "http://localhost:11434";
        await SettingService.SaveSetting("Ollama:Url", _ollamaUrl);
        await CheckOllamaStatus();
    }

    private async Task OnOllamaModelChanged(ChangeEventArgs e)
    {
        _ollamaModel = e.Value?.ToString() ?? "qwen2.5:3b";
        await SettingService.SaveSetting("Ollama:Model", _ollamaModel);
        await CheckOllamaStatus();
    }

    private async Task LoadTags()
    {
        AvailableTags = (await VideoService.GetTagInfos()).Select(x => x.Tag).ToList();
    }

    private async Task LoadSettings()
    {
        AllSettings = (await SettingService.GetAllSettings()).ToList();
    }

    // Synonym editor fields
    private Dictionary<string, List<string>> _synonymMap = new();
    private string _newCanonical = "";
    private string _newSynonyms = "";
    private bool _normalizing;
    private string? _synonymMessage;
    private bool _synonymSuccess;

    private void AddSynonymEntry()
    {
        _synonymMessage = null;
        if (string.IsNullOrWhiteSpace(_newCanonical) || string.IsNullOrWhiteSpace(_newSynonyms)) return;
        var synonyms = _newSynonyms.Split(',', StringSplitOptions.RemoveEmptyEntries).Select(s => s.Trim()).Where(s => s.Length > 0).ToList();
        if (synonyms.Count == 0) return;

        if (_synonymMap.ContainsKey(_newCanonical))
        {
            _synonymMap[_newCanonical] = _synonymMap[_newCanonical].Union(synonyms).ToList();
        }
        else
        {
            _synonymMap[_newCanonical] = synonyms;
        }
        _newCanonical = "";
        _newSynonyms = "";
    }

    private void RemoveSynonymEntry(string canonical)
    {
        _synonymMap.Remove(canonical);
    }

    private async Task SaveSynonyms()
    {
        _synonymMessage = null;
        try
        {
            await TagNormalizationService.SaveSynonymMap(_synonymMap);
            _synonymSuccess = true;
            _synonymMessage = "Synonym map saved successfully.";
        }
        catch
        {
            _synonymSuccess = false;
            _synonymMessage = "Error saving synonym map.";
        }
    }

    private async Task NormalizeAllTags()
    {
        _synonymMessage = null;
        _normalizing = true;
        try
        {
            await TagNormalizationService.NormalizeAllExistingTags();
            _synonymSuccess = true;
            _synonymMessage = "All existing tags normalized.";
        }
        catch
        {
            _synonymSuccess = false;
            _synonymMessage = "Error normalizing tags.";
        }
        finally
        {
            _normalizing = false;
        }
    }

    // Password change fields
    private static readonly PasswordHasher<string> _hasher = new();
    private string _oldPassword = "";
    private string _newPassword = "";
    private string _confirmNewPassword = "";
    private bool _changingPassword;
    private string? _passwordMessage;
    private bool _passwordSuccess;

    private async Task ChangePassword()
    {
        _passwordMessage = null;
        _passwordSuccess = false;

        if (string.IsNullOrWhiteSpace(_oldPassword))
        {
            _passwordMessage = "Please enter your current password.";
            return;
        }

        if (string.IsNullOrWhiteSpace(_newPassword) || _newPassword.Length < 4)
        {
            _passwordMessage = "New password must be at least 4 characters.";
            return;
        }

        if (_newPassword != _confirmNewPassword)
        {
            _passwordMessage = "New passwords do not match.";
            return;
        }

        _changingPassword = true;
        try
        {
            var setting = await SettingService.GetSetting("AuthPasswordHash");
            var hash = setting?.Value ?? "";
            if (string.IsNullOrEmpty(hash))
            {
                _passwordMessage = "No password set. Please use the initial setup.";
                return;
            }

            var result = _hasher.VerifyHashedPassword("admin", hash, _oldPassword);
            if (result == PasswordVerificationResult.Failed)
            {
                _passwordMessage = "Wrong current password.";
                return;
            }

            var newHash = _hasher.HashPassword("admin", _newPassword);
            await SettingService.SaveSetting("AuthPasswordHash", newHash);

            _passwordSuccess = true;
            _passwordMessage = "Password changed successfully.";
            _oldPassword = "";
            _newPassword = "";
            _confirmNewPassword = "";
        }
        catch
        {
            _passwordMessage = "An error occurred while changing the password.";
        }
        finally
        {
            _changingPassword = false;
        }
    }
}
